# Development dependencies (OPTIONAL - not required for runtime operation)
# These are only needed for development, testing, and code quality checks.
# For production installation, use requirements.txt instead.
#
# To install runtime dependencies only: pip install -r requirements.txt
# To install with dev dependencies: pip install -r requirements-dev.txt
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.7.0
mypy>=1.11.0

# Type stubs
types-requests>=2.31.0
types-PyYAML>=6.0.0

# Security scanning
bandit>=1.7.0
pip-audit>=2.7.0

# Pre-commit hooks
pre-commit>=3.6.0
markdownlint-cli>=0.38.0
yamllint>=1.33.0
detect-secrets>=1.4.0
shellcheck-py>=0.9.0.6
