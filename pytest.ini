[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    security: Security-critical tests (canonicalization, evasion)
    integration: Integration tests requiring full stack
    slow: Slow tests (ablation, large datasets)
    xfail: Expected failures (demonstrates requirements)

# Output
addopts = 
    -v
    --tb=short
    --strict-markers

# Ignore patterns
norecursedirs = .git __pycache__ *.egg-info results artifacts

# Coverage (optional)
# addopts = --cov=src/llm_firewall --cov-report=term-missing
