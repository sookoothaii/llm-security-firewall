# LLM Security Firewall - Runtime Requirements (Production)
# Version: 1.0.0
# Python: >=3.12
#
# NOTE: This file contains ONLY runtime dependencies required for operation.
# For development dependencies (pytest, ruff, mypy, etc.), see requirements-dev.txt
# To install runtime dependencies only: pip install -r requirements.txt
# To install with dev dependencies: pip install -r requirements-dev.txt

# Core dependencies
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pyyaml>=6.0
blake3>=0.3.0
requests>=2.31.0
pydantic>=2.0.0  # Data validation for hak_gal.core.session_manager

# Database (choose one or both)
sqlalchemy>=2.0.0           # ORM Layer (REQUIRED for storage.py)
psycopg[binary]>=3.1.0      # PostgreSQL (production)
# sqlite3 is built-in to Python  # SQLite (development)

# Multi-layer detection (NEW)
sentence-transformers>=2.2.0    # Embedding detector (Layer B: Semantic Guard for Kids Policy HYDRA-05)
torch>=2.0.0                    # Perplexity detector
transformers>=4.30.0            # GPT-2 for perplexity
onnx>=1.14.0                    # ONNX model format
onnxruntime>=1.16.0             # ONNX inference (Layer 15 Crisis Detection + GuardNet)

# Rate Limiting (P1) & Security (P2)
redis>=5.0.0                    # Redis for per-tenant rate limiting (v2.3.3)
cryptography>=41.0.0            # AES-GCM encryption for log redaction (P2)

# Optional: Monitoring
# prometheus-client>=0.17.0

# Solo-Dev Status CLI (optional, but recommended)
rich>=13.0.0                    # Beautiful terminal output for status dashboard
psutil>=5.9.0                   # System and process utilities (for system_audit.py)

# Development dependencies
# pytest>=7.4.0
# pytest-cov>=4.1.0
